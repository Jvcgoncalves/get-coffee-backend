import express, { Request, Response } from "express";

const reviewsRouter = express.Router();

reviewsRouter.post("register", async (request: Request, response: Response) => {

})

export default reviewsRouter;